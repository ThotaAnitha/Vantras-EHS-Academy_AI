services:
  vantras:
    build:
      context: ..
      dockerfile: deployment/Dockerfile
    ports:
      - "8173:8173"
    environment:
      VANTRAS_HOST: 0.0.0.0
      VANTRAS_NO_BROWSER: "1"
    volumes:
      - ../database:/app/database
